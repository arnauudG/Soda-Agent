# Packages

This folder is for large, vendor-provided artifacts that are required by some stacks but should not be committed to git.

- `packages/collibra-dq/`: Collibra DQ installer package expected by the package upload module.

See `packages/collibra-dq/README.md`.
